import {PlanNullable} from "@/types/plan";
import {PlanContext} from "@/contexts/PlanContext";
import { useContext } from "react"

export default function SemLogic() {
    const { plan, setPlan } = useContext(PlanContext);
}